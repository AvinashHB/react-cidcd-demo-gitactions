name: Deploy dist
on: push
jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      run: |
        echo "Cloning repo"
        echo "${{github.repository}}"
        git clone https://github.com/${{github.repository}}.git myfiles
        cd myfiles && ls