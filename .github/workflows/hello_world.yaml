name: Greet user
on: workflow_dispatch
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the user
        run: echo "Hello, ${{ github.actor }}!"
      - name: Say goodbye
        run: echo "Bye, ${{ github.actor }}!"